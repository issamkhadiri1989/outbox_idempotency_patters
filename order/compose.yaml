services:
  nginx:
    build:
      context: ./infra/
      args:
        BUILD_ARGUMENT_ENV: ${APP_ENV:-dev}
      dockerfile: ./nginx/Dockerfile
    restart: unless-stopped
    volumes:
      - ./src:/var/www/html:ro,cached
      - ./infra/nginx/conf.d:/etc/nginx/conf.d
    depends_on:
      php:
        condition: service_started
    networks:
      - order_internal_network
      - proxy-net
    expose:
      - "80"
    environment:
      SERVER_NAME: "http://:80"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.order.rule=Host(`${HOST}`)"
      - "traefik.http.routers.order.entrypoints=websecure"
      - "traefik.http.routers.order.tls=true"
      - "traefik.http.services.order.loadbalancer.server.port=80"
      - "traefik.http.services.order.loadbalancer.server.scheme=http"
      - "traefik.docker.network=proxy-net"

  php:
    build:
      context: ./infra/
      dockerfile: ./php/Dockerfile
    restart: unless-stopped
    volumes:
      - ./src:/var/www/html:cached
      - ./infra/php/php-fpm.d/www.conf:/usr/local/etc/php-fpm.d/www.conf
      - ./infra/php/config/php.ini:/usr/local/etc/php/php.ini
      - ./infra/php/xdebug/xdebug.ini:/usr/local/etc/php/conf.d/xdebug.ini
    depends_on:
      database:
        condition: service_healthy
    networks:
      - order_internal_network
      - proxy-net
    expose:
      - "9000"
      - "9003"

  database:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DATABASE_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DATABASE_NAME}
    volumes:
        - order_mysql:/var/lib/mysql:delegated
    networks:
        - order_internal_network
    healthcheck:
      test:
        [
          "CMD",
          "mysqladmin",
          "ping",
          "-h",
          "localhost",
          "-u",
          "root",
          "-p$$MYSQL_ROOT_PASSWORD",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  phpmyadmin:
      image: phpmyadmin/phpmyadmin:5
      environment:
          PMA_HOST: database
      depends_on:
          - database
      networks:
          - order_internal_network
          - proxy-net
      volumes:
          - order__db_admin_data:/var/www/html
      labels:
          - "traefik.enable=true"
          - "traefik.http.routers.order-pma.rule=Host(`${PMA_HOST}`)"
          - "traefik.http.routers.order-pma.entrypoints=websecure"
          - "traefik.http.routers.order-pma.tls=true"
          - "traefik.http.services.order-pma.loadbalancer.server.port=80"
          - "traefik.docker.network=proxy-net"

volumes:
    order__db_admin_data:
        name: order_db_admin_data
    order_mysql:
        name: order_mysql

networks:
    order_internal_network:
        name: internal_network_order
    proxy-net:
        name: proxy-net
        external: true
